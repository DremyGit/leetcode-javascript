import climbStairs from './';

test('Example 1', () => {
  expect(climbStairs(2)).toBe(2);
});

test('Example 1', () => {
  expect(climbStairs(3)).toBe(3);
});

test('Example 1', () => {
  expect(climbStairs(4)).toBe(5);
});

test('Example 1', () => {
  expect(climbStairs(60)).toBe(2504730781961);
});
